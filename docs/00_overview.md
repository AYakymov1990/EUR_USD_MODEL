# Обзор проекта (Trader CRM)

## Что уже есть
- Датасет M15/H1 EURUSD с вычисленными признаками (`data/eurusd_features.parquet`).
- Пайплайн признаков/таргета в `src/features.py` и проверка выравнивания.
- Модель (MLP) и обучение/валидация в ноутбуках `02_features_and_target.ipynb`, `03_model_and_backtest.ipynb`.
- Бектест и метрики в `src/backtest.py`.
- Выбор лучшего конфига (threshold/regime/sizing/hold_bars) и сохранение `data/artifacts/selected_config.json`.

## Что нужно добавить (демо до 15 января)
- CRM-слой поверх модели: генерация сигналов, объяснение, логирование, подтверждение человеком.
- Интеграция с OANDA v20 (practice по умолчанию), опционально live.
- Demo mode без сети: реплей свечей/фичей из тестового среза, сигнал → лог → UI.
- Streamlit UI: последний сигнал и объяснение, кнопки LONG/SHORT, состояние счёта, мини-метрики.

## Ограничения
- Архитектуру модели и обучение не меняем (заморожено).
- Конфиги/ключи только через `.env` или переменные окружения, без секретов в репо.
- Безопасность: исполнение в demo mode по умолчанию; live — только с явным флагом.
